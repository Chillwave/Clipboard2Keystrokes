# Clipboard2Keystrokes
Python script to simulate typing out the contents of the clipboard using insert as the shortcut. Inital use case was for typing in passwords on web-based KVMs.

* Works with NoVNC based KVMs. This script remedies having to manually type a long password to recover access to a machine.

## Usage

* Run script, copy text, press insert to simulate keystroke equivalent of clipboard content.
